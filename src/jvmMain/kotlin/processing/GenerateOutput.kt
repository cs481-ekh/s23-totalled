package processing

/**
 * Placeholder, rename package/file/function if needed.
 */
fun generateOutput(inputFilePath: String, outputDirPath: String) {
    println(inputFilePath)
    println(outputDirPath)
    Thread.sleep(1000)
    throw Exception("Output generation not implemented")
}
